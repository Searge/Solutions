for i from 0 to n-1
    *find* _smallest_ element between `i`'th and `n-1`'th 
    *swap* _smallest_ with `i`'th element

    `n(n - 1) / 2` or `O(n^2)`
    code::
    	(n^2 – n)/2
		n^2/2 – n/2

для і від 0 до n-1
	знайти найменший елемент між і-тим та n-1-им
	обміняти найменший з і-тим елементом